//
// src/another.cpp
//

#include "src/another.h"

int Another::Product(const int &a, const int &b) const {
    return a * b;
}

int Another::Sum(const int &a, const int &b) const {
    return a + b;
}
